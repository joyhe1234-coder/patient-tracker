# Requirements Document: Module 7 — Filtering & Search Comprehensive Test Plan

## Introduction

This specification consolidates all testable behaviors from five existing feature specs -- status-filter, multi-select-filter, compact-filter-bar, patient-name-search, and insurance-group -- into a unified, comprehensive test plan for Module 7: Filtering & Search. The goal is to map every testable behavior to acceptance criteria, identify coverage gaps versus current automated tests, and propose new test cases to fill those gaps. This is a test-only module; no production code changes are required.

### Current Test Inventory (Module 7 scope)

| Layer | File | Test Count | Coverage Area |
|-------|------|-----------|---------------|
| Vitest | `StatusFilterBar.test.tsx` | 115 (+ 66 from it.each = **181**) | Chip rendering, multi-select toggle, checkmark/fill style, search input, QM dropdown, insurance dropdown, zero-count chips, compact styling, pinned row badge, `getRowStatusColor` mappings, CFB-R8 accuracy groups A-G |
| Vitest | `StatusBar.test.tsx` | 18 | Row count display, filter summary, connection status, presence indicator |
| Vitest | `MainPage.test.tsx` | 67 | `filterRows` logic (name search, status filter, multi-select OR, QM filter, combined AND, pinned row bypass, measure-scoped rowCounts, insurance group API/query/summary) |
| Cypress | `multi-select-filter.cy.ts` | 18 | Multi-select toggle, Duplicates exclusivity, checkmark style, search+multi-filter, status bar, keyboard a11y |
| Cypress | `patient-name-search.cy.ts` | 14 | Search UI, filtering behavior, clear, filter combo AND logic, status bar count, keyboard shortcuts |
| Cypress | `insurance-group-filter.cy.ts` | 12 | Dropdown rendering, visual indicators, filtering behavior, persistence/interaction |
| Cypress | `compact-filter-bar.cy.ts` | 3 | Grid row filtering by measure, chip count update, measure+color AND |
| Cypress | `sorting-filtering.cy.ts` | 56 | Status filter bar chips (display, counts, per-color filter, toggle, sort+filter, status bar, overdue row colors) |
| Cypress | `filter-roles-combined.cy.ts` | **24** | Role-based filtering (PHYSICIAN/STAFF), combined 4-filter AND, chip count updates after edits, edge cases (special chars, rapid switching, No Insurance) |
| Playwright | `compact-filter-bar.spec.ts` | 5 | Compact chip styling, measure dropdown, combined filtering, zero-count faded chip |
| **TOTAL** | | **~398** | |

> **Note on test counts:** `StatusFilterBar.test.tsx` contains 115 explicit `it()` calls plus 42 tests generated by `it.each(statusToExpectedColor)` (Group A exhaustive mapping) and 24 tests generated by `it.each(representativeRows)` (Group G cross-system consistency), totaling 181 effective test cases. The `sorting-filtering.cy.ts` count includes 7 tests generated by `forEach` loops (4 green + 3 blue status verification). The `filter-roles-combined.cy.ts` was added in commit 6e5730a and was not included in the original inventory.

### Source Specifications

- `.claude/specs/status-filter/requirements.md` (AC-1 through AC-8, EC-1 through EC-5)
- `.claude/specs/multi-select-filter/requirements.md` (AC-1.1 through AC-5.3)
- `.claude/specs/compact-filter-bar/requirements.md` (CFB-R1 through CFB-R8)
- `.claude/specs/patient-name-search/requirements.md` (Req-1 through Req-5)
- `.claude/specs/insurance-group/requirements.md` (REQ-IG-1 through REQ-IG-8)

### Key Implementation Files

| Component | File | Role |
|-----------|------|------|
| StatusFilterBar | `frontend/src/components/layout/StatusFilterBar.tsx` | Color chips, QM dropdown, insurance group dropdown, search input |
| MainPage | `frontend/src/pages/MainPage.tsx` | Filter state (useState), filteredRowData (useMemo), rowCounts (useMemo), filterSummary (useMemo), API query params with insuranceGroup |
| StatusBar | `frontend/src/components/layout/StatusBar.tsx` | "Showing X of Y rows" + filter summary display |
| statusColors | `frontend/src/config/statusColors.ts` | `getRowStatusColor()`, color arrays, overdue logic |
| dropdownConfig | `frontend/src/config/dropdownConfig.ts` | `QUALITY_MEASURE_TO_STATUS`, measure options |

## Alignment with Product Vision

This test plan directly supports the product vision by ensuring the correctness of the filtering and search subsystem -- the primary mechanism for clinical staff to focus on specific patient subsets across the quality measure tracking grid. The filter bar provides "real-time compliance visibility" (product.md success metric) by allowing users to quickly isolate patients by status color, insurance group, quality measure, and name. Gaps in test coverage here represent risks to the core workflow where physicians and staff triage their patient lists daily.

---

## Requirements

### REQ-M7-1: Status Color Filter (Multi-Select Toggle)

**User Story:** As a QA engineer, I want every aspect of the status color chip filter verified -- rendering, multi-select toggle ON/OFF, Duplicates exclusivity, All reset, visual styling, aria attributes, and count badges -- so that no filter interaction regression can go undetected.

#### Acceptance Criteria

1. **AC-M7-1.1:** WHEN the filter bar renders, THEN 10 chips SHALL appear in this order: All, Duplicates, Not Addressed, Overdue, In Progress, Contacted, Completed, Declined, Resolved, N/A.
2. **AC-M7-1.2:** WHEN any chip is rendered, THEN it SHALL display the status label and a count in parentheses in format "Label (X)".
3. **AC-M7-1.3:** WHEN the page loads, THEN the "All" chip SHALL be active (aria-pressed="true") and all other chips SHALL be inactive (aria-pressed="false").
4. **AC-M7-1.4:** WHEN user clicks an unselected status chip, THEN it SHALL be added to the active filters (toggled ON) without deselecting other active chips (multi-select OR logic).
5. **AC-M7-1.5:** WHEN user clicks an already-selected status chip, THEN it SHALL be removed from the active filters (toggled OFF) without affecting other active chips.
6. **AC-M7-1.6:** WHEN the last individual status color chip is toggled OFF (and Duplicates is not active), THEN the "All" chip SHALL automatically become active (preventing zero-selection state).
7. **AC-M7-1.7:** WHEN user clicks the "All" chip, THEN all individual filter selections SHALL be cleared and "All" SHALL become the only active filter.
8. **AC-M7-1.8:** WHEN "All" is active and user clicks any individual chip, THEN "All" SHALL be deselected and only the clicked chip SHALL become active.
9. **AC-M7-1.9:** WHEN user selects the "Duplicates" chip, THEN all status color chips SHALL be deselected and only duplicates SHALL be shown (exclusive mode).
10. **AC-M7-1.10:** WHEN user selects any status color chip while Duplicates is active, THEN Duplicates SHALL be deselected and the color filter SHALL become active.
11. **AC-M7-1.11:** WHEN Duplicates is toggled OFF, THEN the filter SHALL return to "All" (fallback).
12. **AC-M7-1.12:** WHEN multiple chips are active, THEN the grid SHALL show all rows matching ANY of the selected status colors (OR logic between chips).
13. **AC-M7-1.13:** WHEN a chip is selected, THEN it SHALL display with a filled background, the status color identity, and a checkmark (Check) icon as leading element.
14. **AC-M7-1.14:** WHEN a chip is unselected, THEN it SHALL display with a white background, bordered style, and no checkmark.
15. **AC-M7-1.15:** WHEN a chip has focus and user presses Enter or Space, THEN the chip SHALL toggle its selected state (keyboard accessibility).
16. **AC-M7-1.16:** WHEN navigating with Tab key, THEN focus SHALL move through chips in order.
17. **AC-M7-1.17:** WHEN a chip's toggle state changes, THEN its `aria-pressed` attribute SHALL be updated to reflect the current state ("true" when selected, "false" when not).

#### Coverage Gap Analysis (REQ-M7-1)

| Gap ID | Description | Current State | Status |
|--------|------------|---------------|--------|
| GAP-1.1 | Combined multi-select + Duplicates exclusivity sequence not tested as a single E2E flow | Cypress tests both separately; no combined toggle sequence | **OPEN** (MEDIUM) -- Add Cypress test: select 2 color chips, then Duplicates (verify colors deselected), then color chip (verify Duplicates deselected), then toggle off last chip (verify All) |
| GAP-1.2 | Rapid chip clicking (multiple toggle events in quick succession) not tested | **CLOSED** by `filter-roles-combined.cy.ts`: `rapid filter switching does not break the grid` | **CLOSED** |
| GAP-1.3 | Multi-select chip state after grid data changes (row added/deleted/edited) not tested | Merged into GAP-7.5 (data refresh preserves filter state) | **MERGED** into GAP-7.5 |

---

### REQ-M7-2: Chip Count Badges and Accuracy

**User Story:** As a QA engineer, I want chip count badges verified for accuracy across all filter dimensions (unfiltered, measure-scoped, after data changes), so that users can trust the count numbers displayed on each chip.

#### Acceptance Criteria

1. **AC-M7-2.1:** WHEN "All Measures" is selected, THEN each chip count SHALL reflect the total number of rows for that status color from the full dataset.
2. **AC-M7-2.2:** WHEN a specific quality measure is selected, THEN each chip count SHALL be recalculated to reflect only rows matching that measure.
3. **AC-M7-2.3:** WHEN a specific quality measure is selected, THEN the "All" chip count SHALL show the total number of rows for that measure (not the overall total).
4. **AC-M7-2.4:** WHEN name search text is active alongside filters, THEN chip counts SHALL NOT be affected by the search text (counts reflect the measure-scoped dataset, not the search-filtered dataset).
5. **AC-M7-2.5:** WHEN a row's status is edited causing its color to change, THEN chip counts SHALL update immediately to reflect the change.
6. **AC-M7-2.6:** WHEN a row is overdue (displays as red), THEN the chip count logic SHALL count that row under the "red" chip, NOT under its "natural" status color.
7. **AC-M7-2.7:** WHEN a row is both a duplicate AND overdue, THEN the chip count logic SHALL count that row under BOTH the "duplicate" chip AND the "red" chip.
8. **AC-M7-2.8:** WHEN the "All" chip displays a total count, THEN that count SHALL equal the sum of all status color counts (white + yellow + blue + green + purple + orange + gray + red), excluding the duplicate count to avoid double-counting.
9. **AC-M7-2.9:** WHEN a chip has a count of zero (because no rows have that status in the current measure scope), THEN the chip SHALL display with a dashed border style but SHALL remain clickable.
10. **AC-M7-2.10:** WHEN a zero-count chip is clicked, THEN it SHALL become selected (with active styling) and the grid SHALL show zero rows.

#### Coverage Gap Analysis (REQ-M7-2)

| Gap ID | Description | Current State | Status |
|--------|------------|---------------|--------|
| GAP-2.1 | Chip counts accuracy when multiple filters are active simultaneously (color + measure) not tested E2E | **PARTIALLY CLOSED** -- `compact-filter-bar.cy.ts` tests chip counts update with measure; `filter-roles-combined.cy.ts` verifies `sum of all color chip counts equals All chip count` and `chip counts stay consistent with grid row count after filter` | **CLOSED** (3 tests in FRC cover chip count integrity) |
| GAP-2.2 | Chip counts after row edit (status change) not deeply tested E2E | FRC has `editing measureStatus changes chip counts` but does not assert specific increment/decrement delta | **OPEN** (HIGH) -- Strengthen test to verify source chip decrements and target increments |
| GAP-2.3 | Chip counts after add/delete row not tested E2E | Merged into GAP-2.2 (edit covers the re-count mechanism; add/delete uses same path) | **MERGED** into GAP-2.2 |
| GAP-2.4 | Zero-count chip behavior at E2E layer not tested | Only tested at Vitest (dashed border) and Playwright (faded opacity); no Cypress click+empty-grid test | **OPEN** (MEDIUM) -- Add Cypress test: select measure where a color has 0 rows, click that chip, verify empty grid |

---

### REQ-M7-3: Insurance Group Filter

**User Story:** As a QA engineer, I want the insurance group dropdown filter verified for rendering, default value, server-side filtering, visual indicators, and interaction with other filters, so that per-group filtering works correctly across all scenarios.

#### Acceptance Criteria

1. **AC-M7-3.1:** WHEN the filter bar renders, THEN an insurance group dropdown SHALL appear after the Quality Measure dropdown, with `aria-label="Filter by insurance group"`.
2. **AC-M7-3.2:** WHEN the dropdown is rendered, THEN options SHALL be in order: "All" first, then system names from `GET /api/import/systems` sorted alphabetically, then "No Insurance" last.
3. **AC-M7-3.3:** WHEN any authenticated user (ADMIN, PHYSICIAN, STAFF) loads the page, THEN the insurance group dropdown SHALL default to "Hill" (value `hill`).
4. **AC-M7-3.4:** WHEN the user selects a specific insurance group, THEN the system SHALL pass `insuranceGroup` as a query parameter to `GET /api/data` (server-side filtering).
5. **AC-M7-3.5:** WHEN the user selects "No Insurance", THEN the system SHALL pass `insuranceGroup=none` to `GET /api/data`.
6. **AC-M7-3.6:** WHEN the user selects "All", THEN the system SHALL omit the `insuranceGroup` query parameter (no filtering).
7. **AC-M7-3.7:** WHEN the insurance group filter is active (not "All"), THEN the dropdown SHALL have a blue ring/border highlight indicator.
8. **AC-M7-3.8:** WHEN the insurance group filter is "All", THEN the dropdown SHALL NOT have the blue ring highlight.
9. **AC-M7-3.9:** WHEN the insurance group filter changes, THEN status color chip counts SHALL update to reflect the newly fetched data.
10. **AC-M7-3.10:** WHEN the `GET /api/import/systems` endpoint fails, THEN the dropdown SHALL fall back to hardcoded options: "All", "Hill", "No Insurance".
11. **AC-M7-3.11:** WHEN the insurance group filter is changed, THEN the active color chip selections, measure dropdown selection, and search text SHALL be preserved.

#### Coverage Gap Analysis (REQ-M7-3)

| Gap ID | Description | Current State | Status |
|--------|------------|---------------|--------|
| GAP-3.1 | Insurance group + color filter + name search triple-AND combination not tested E2E | **CLOSED** by FRC: `applies all 4 filters simultaneously (color + QM + insurance + search)` covers this as part of the 4-filter test | **CLOSED** |
| GAP-3.2 | Insurance group filter with "No Insurance" E2E not deeply tested | **CLOSED** by FRC: `"No Insurance" filter shows correct rows or empty grid` | **CLOSED** |
| GAP-3.3 | Insurance group dropdown fallback on API failure not tested E2E | Only tested at Vitest level (MainPage.test.tsx) | **OPEN** (MEDIUM) -- Add Playwright test: intercept `/api/import/systems` with error, verify dropdown shows fallback options |
| GAP-3.4 | Chip counts update after insurance group change not tested E2E | **CLOSED** by FRC: `insurance group + color chip: switching insurance group updates chip counts` | **CLOSED** |

---

### REQ-M7-4: Patient Name Search

**User Story:** As a QA engineer, I want the patient name search input verified for rendering, real-time filtering, case-insensitivity, partial matching, multi-word matching, clear behavior, keyboard shortcuts, and interaction with other filters, so that name-based patient lookup works in all scenarios.

#### Acceptance Criteria

1. **AC-M7-4.1:** WHEN the main page loads, THEN a search input SHALL appear in the filter bar with placeholder text "Search by name..." and a search icon (magnifying glass) on the left side.
2. **AC-M7-4.2:** WHEN the search input is empty, THEN no name filtering SHALL be applied and the clear (X) button SHALL NOT be visible.
3. **AC-M7-4.3:** WHEN the user types in the search input, THEN the grid SHALL filter to show only rows whose `memberName` contains the search text (case-insensitive, substring match).
4. **AC-M7-4.4:** WHEN the search text contains multiple words (space-separated), THEN ALL words SHALL match independently against the memberName (each word must be present somewhere in the name).
5. **AC-M7-4.5:** WHEN the search text matches zero patients, THEN the grid SHALL show an empty state (zero rows).
6. **AC-M7-4.6:** WHEN the user clears the search text (via clear button or manual deletion), THEN all rows SHALL be restored (respecting active color/measure/insurance filters).
7. **AC-M7-4.7:** WHEN the search input has text, THEN a clear (X) button SHALL appear on the right side.
8. **AC-M7-4.8:** WHEN the user clicks the clear button, THEN search text SHALL be cleared and all rows restored.
9. **AC-M7-4.9:** WHEN the user presses Ctrl+F (or Cmd+F on Mac) while not editing a cell, THEN the search input SHALL receive focus.
10. **AC-M7-4.10:** WHEN the search input is focused and the user presses Escape, THEN the search text SHALL be cleared and the input SHALL lose focus.
11. **AC-M7-4.11:** WHEN both a name search and status color filter are active, THEN rows SHALL pass BOTH criteria (AND logic).
12. **AC-M7-4.12:** The search input SHALL have `aria-label="Search patients by name"` and the clear button SHALL have `aria-label="Clear search"`.

#### Coverage Gap Analysis (REQ-M7-4)

| Gap ID | Description | Current State | Status |
|--------|------------|---------------|--------|
| GAP-4.1 | Multi-word search ("williams robert") matching "Williams, Robert" not tested E2E | Vitest tests multi-word; no Cypress test | **OPEN** (MEDIUM) -- Add Cypress test: read a name from grid, type "lastname firstname", verify matching row appears |
| GAP-4.2 | Ctrl+F intercepted during AG Grid cell editing not tested | Vitest tests Ctrl+F focus; no test for popup-editor guard | **DEPRIORITIZED** -- Ctrl+F guard is implementation-specific and fragile in E2E; Ctrl+F focus itself is well-tested in `patient-name-search.cy.ts` |
| GAP-4.3 | Search with extra whitespace (leading, trailing, between words) not tested E2E | Vitest tests whitespace trimming; no Cypress test | **OPEN** (MEDIUM) -- Add Cypress test: type " smith  john " with extra spaces, verify filtering works correctly |
| GAP-4.4 | Search text preserved when color filter changes not tested E2E | Only tested at Vitest level | **OPEN** (MEDIUM) -- Add Cypress test: type search term, click color chip, verify search input still shows term + combined filtering |

---

### REQ-M7-5: Quality Measure Filter

**User Story:** As a QA engineer, I want the quality measure dropdown filter verified for rendering, option list completeness, filtering behavior, visual indicator, and interaction with other filters, so that measure-based filtering works correctly for all 13+ quality measures.

#### Acceptance Criteria

1. **AC-M7-5.1:** WHEN the filter bar renders, THEN a dropdown labeled via `aria-label="Filter by quality measure"` SHALL appear after the last color chip, separated by a vertical divider.
2. **AC-M7-5.2:** WHEN the dropdown is in its default state, THEN it SHALL display "All Measures".
3. **AC-M7-5.3:** WHEN the dropdown is clicked, THEN it SHALL display "All Measures" plus all 14 quality measures from `QUALITY_MEASURE_TO_STATUS` configuration (including Depression Screening), sorted alphabetically.
4. **AC-M7-5.4:** WHEN the user selects a specific quality measure, THEN the grid SHALL filter to show only rows where `qualityMeasure` matches the selected value.
5. **AC-M7-5.5:** WHEN a specific quality measure is selected, THEN the dropdown SHALL display a blue border highlight (ring-2 ring-blue-400).
6. **AC-M7-5.6:** WHEN "All Measures" is selected (default), THEN the dropdown SHALL NOT have the blue border highlight.
7. **AC-M7-5.7:** WHEN a specific quality measure is selected, THEN chip counts SHALL be scoped to only rows matching that measure (measure-scoped counts).
8. **AC-M7-5.8:** WHEN the user selects "All Measures", THEN the measure filter SHALL be cleared and all rows SHALL be shown (subject to other active filters).
9. **AC-M7-5.9:** WHEN a measure filter is active AND color chips are selected, THEN a row SHALL appear only if its quality measure matches AND its status color matches one of the selected chips (AND + OR logic).

#### Coverage Gap Analysis (REQ-M7-5)

| Gap ID | Description | Current State | Status |
|--------|------------|---------------|--------|
| GAP-5.1 | All 14 quality measures individually tested as filter options E2E | Cypress compact-filter-bar tests 1 measure; no comprehensive per-measure test | **DEPRIORITIZED** -- Per-measure iteration is brittle against seed data changes; Vitest covers the filterRows logic for all measures. The single AWV E2E test provides sufficient integration confidence. |
| GAP-5.2 | Measure filter with Duplicates chip combination not tested E2E | Vitest tests `duplicate filter + measure applies AND logic`; merged into GAP-6.4 | **MERGED** into GAP-6.4 (Duplicates + measure + search triple AND) |
| GAP-5.3 | Measure dropdown option count validation not tested E2E | Only Vitest tests option count (verifies 15 options) | **OPEN** (MEDIUM) -- Add Cypress test: open measure dropdown, count options, verify 15 total |

---

### REQ-M7-6: Combined Filter Scenarios (Color + Insurance + Search + QM)

**User Story:** As a QA engineer, I want the interaction between all four filter dimensions tested simultaneously, so that complex real-world filter combinations produce correct results with proper AND/OR logic.

#### Acceptance Criteria

1. **AC-M7-6.1:** WHEN multiple filter types are active (color chips, measure dropdown, insurance group, and/or name search), THEN the grid SHALL show only rows matching ALL active filters simultaneously: `(colorA OR colorB) AND measureMatch AND insuranceGroupMatch AND searchMatch`.
2. **AC-M7-6.2:** WHEN the user changes one filter dimension, THEN all other filter dimensions SHALL be preserved and the combined filter re-applied.
3. **AC-M7-6.3:** WHEN color chips use OR logic between selected chips, THEN the combined filter SHALL be: `(colorA OR colorB) AND measureMatch AND searchMatch` (insurance group is server-side, already applied to the fetched dataset).
4. **AC-M7-6.4:** WHEN Duplicates chip is selected AND a measure filter is active AND search text is entered, THEN the grid SHALL show only rows that are duplicates AND match the quality measure AND match the search text.
5. **AC-M7-6.5:** WHEN all four filters are active AND the result set is zero rows, THEN the grid SHALL display an empty state and the status bar SHALL show "Showing 0 of Y rows".
6. **AC-M7-6.6:** WHEN a color filter AND a measure filter are both active, THEN the status bar filter summary SHALL include both (e.g., "Color: In Progress | Measure: Diabetic Eye Exam").
7. **AC-M7-6.7:** WHEN insurance group, color, and measure filters are all active, THEN the filter summary SHALL include all three (e.g., "Insurance: Hill | Color: Completed | Measure: AWV").
8. **AC-M7-6.8:** WHEN "All" color, "All Measures", and "All" insurance are selected with empty search, THEN no filter summary SHALL be displayed.

#### Coverage Gap Analysis (REQ-M7-6)

| Gap ID | Description | Current State | Status |
|--------|------------|---------------|--------|
| GAP-6.1 | Triple-AND combination (color + measure + search) not tested E2E | **CLOSED** by FRC: `applies all 4 filters simultaneously` includes color + QM + search (plus insurance) | **CLOSED** |
| GAP-6.2 | Quadruple combination (insurance + color + measure + search) not tested at any layer | **CLOSED** by FRC: `applies all 4 filters simultaneously (color + QM + insurance + search)` | **CLOSED** |
| GAP-6.3 | Filter summary text with all four dimensions active not tested E2E | Vitest tests filterSummary for insurance+color, insurance+measure; no E2E for triple or quad | **OPEN** (HIGH) -- Merged into GAP-8.6 for triple filter summary |
| GAP-6.4 | Empty result set from combined filters not tested E2E | **PARTIALLY CLOSED** by FRC: `applies all 4 filters simultaneously` ends with 0-row assertion via nonexistent search | **OPEN** (HIGH) -- Still need Duplicates + measure + search triple AND as distinct scenario |
| GAP-6.5 | Filter dimension preservation when changing one filter not tested as E2E sequence | **CLOSED** by FRC: `removing one filter restores rows while others stay active` | **CLOSED** |

---

### REQ-M7-7: Filter State Management

**User Story:** As a QA engineer, I want filter state lifecycle verified -- initialization, no persistence across navigation, reset behavior, and state validity constraints -- so that filter state is always predictable and correct.

#### Acceptance Criteria

1. **AC-M7-7.1:** WHEN the user navigates to the main page, THEN the "All" chip SHALL be selected, "All Measures" SHALL be the dropdown default, search input SHALL be empty, and insurance group SHALL default to "Hill".
2. **AC-M7-7.2:** WHEN the user navigates away from the main page and returns, THEN all filter state SHALL be reset to defaults (no persistence).
3. **AC-M7-7.3:** WHEN the user changes the physician selector in the header, THEN the insurance group filter SHALL be preserved (the two filters are independent).
4. **AC-M7-7.4:** WHEN the user changes the insurance group filter, THEN a new data fetch SHALL be triggered from the server (insuranceGroup query parameter changes).
5. **AC-M7-7.5:** WHEN data is refreshed (cell edit, add row, delete row, socket-based refresh), THEN the active filter selections (color chips, measure dropdown, search text) SHALL be preserved and re-applied to the updated dataset.
6. **AC-M7-7.6:** WHEN the user has a pinned row (from add/duplicate) AND changes any filter, THEN the pinned row badge SHALL be cleared (pinnedRowId set to null).
7. **AC-M7-7.7:** WHEN a pinned row exists, THEN it SHALL pass through ALL filter dimensions (color, measure, search) regardless of whether it matches the active filters, until the pin is cleared.

#### Coverage Gap Analysis (REQ-M7-7)

| Gap ID | Description | Current State | Status |
|--------|------------|---------------|--------|
| GAP-7.1 | Filter state reset on navigation (away and back) not tested E2E | **PARTIALLY CLOSED** by FRC: `filter state persists after page navigation and back` -- navigates away and back, verifies grid loads without error and All chip visible | **CLOSED** (FRC test verifies navigation stability; state reset is by design since useState resets on unmount) |
| GAP-7.2 | Physician selector change preserves insurance group filter not tested E2E | FRC-STAFF: `STAFF can switch physician and chip counts update` tests physician switch but not explicit insurance preservation | **OPEN** (MEDIUM) -- Add Cypress test (ADMIN): set insurance to "All", change physician, verify insurance still "All" |
| GAP-7.3 | Pinned row bypass of all filters simultaneously not tested E2E | Vitest tests pinned row bypass for each dimension; no E2E test | **OPEN** (MEDIUM) -- Add Cypress test: add new row (creates pin), select non-matching measure, verify new row still visible |
| GAP-7.4 | Filter change clears pinned row badge not tested E2E | Vitest tests the callback wrapper; no E2E test | **OPEN** (MEDIUM) -- Add Cypress test: add a row (pinned badge appears), change color filter, verify pinned badge disappears |
| GAP-7.5 | Data refresh preserves filter state not tested E2E | Not automated at any layer | **OPEN** (HIGH) -- Add Cypress test: select measure + color filter, edit a cell, verify filter selections preserved after update |
| GAP-7.6 | Sort order preserved when filter applied not tested E2E | `sorting-filtering.cy.ts` has `should maintain filter when sorting` and `should maintain sort when changing filter` but not explicit sorted-order-verification after filter | **OPEN** (MEDIUM) -- Add Cypress test: sort by column, apply color filter, verify filtered rows maintain sort order |

---

### REQ-M7-8: Status Bar Filter Display

**User Story:** As a QA engineer, I want the status bar's "Showing X of Y rows" and filter summary verified for all filter combinations, so that users always see accurate row counts and know which filters are active.

#### Acceptance Criteria

1. **AC-M7-8.1:** WHEN any combination of filters is active, THEN the status bar SHALL display "Showing X of Y rows" where X is the number of rows passing all active filters and Y is the total number of rows in the dataset.
2. **AC-M7-8.2:** WHEN a color filter is active, THEN the filter summary SHALL include the active color labels (e.g., "Color: In Progress, Overdue").
3. **AC-M7-8.3:** WHEN a measure filter is active, THEN the filter summary SHALL include the measure name (e.g., "Measure: Diabetic Eye Exam").
4. **AC-M7-8.4:** WHEN an insurance group filter is active (not "All"), THEN the filter summary SHALL include the insurance group (e.g., "Insurance: Hill").
5. **AC-M7-8.5:** WHEN "All" color, "All Measures", and "All" insurance are all selected, THEN no filter summary text SHALL be displayed (only the row count).
6. **AC-M7-8.6:** WHEN a color filter AND a measure filter AND an insurance filter are all active, THEN the filter summary SHALL show all three, separated by pipe delimiters (e.g., "Insurance: Hill | Color: Completed | Measure: AWV").
7. **AC-M7-8.7:** WHEN the user searches by name (search text is active), THEN the status bar row count (X) SHALL reflect the search-narrowed result set, but the search text SHALL NOT appear in the filter summary.

#### Coverage Gap Analysis (REQ-M7-8)

| Gap ID | Description | Current State | Status |
|--------|------------|---------------|--------|
| GAP-8.1 / GAP-8.6 | Triple-filter summary (insurance + color + measure) not tested E2E | Vitest tests individual and dual combinations; no E2E for triple | **OPEN** (HIGH) -- Add Cypress test: set insurance to non-All, select color chip, select measure, verify status bar shows all three separated by pipes |
| GAP-8.2 | Status bar "Showing 0 of Y rows" with combined filters not tested E2E | **PARTIALLY CLOSED** by FRC: 4-filter test verifies 0 rows with nonexistent search; does not explicitly check status bar text | **DEPRIORITIZED** -- The 0-row state is well-covered by the existing 4-filter test |
| GAP-8.3 | Row count accuracy after search + color + measure combination not tested E2E | **CLOSED** by FRC: `applies all 4 filters simultaneously` + `removing one filter restores rows while others stay active` cover combined filter row counts | **CLOSED** |

---

### REQ-M7-9: Compact Chip Styling

**User Story:** As a QA engineer, I want the compact chip styling (24px height, single-line, no-wrap) verified so that the filter bar is visually correct and does not overflow on standard viewports.

#### Acceptance Criteria

1. **AC-M7-9.1:** WHEN the filter bar renders, THEN each chip SHALL have `white-space: nowrap` so label text never wraps.
2. **AC-M7-9.2:** WHEN the filter bar renders, THEN each chip SHALL have compact padding (`py-0.5 px-2`) and `text-xs` font size.
3. **AC-M7-9.3:** WHEN a chip label contains a space (e.g., "Not Addressed", "In Progress"), THEN it SHALL remain on a single line without truncation.
4. **AC-M7-9.4:** WHEN chips are compact, THEN existing visual treatment SHALL be preserved: checkmark on selected, filled background when selected, bordered when unselected, count in parentheses.
5. **AC-M7-9.5:** WHEN all 10 chips plus dropdowns and search input render on a viewport of 1280px or wider, THEN no elements SHALL overflow or be hidden.

#### Coverage Gap Analysis (REQ-M7-9)

| Gap ID | Description | Current State | Status |
|--------|------------|---------------|--------|
| GAP-9.1 | Narrow viewport (below 1280px) wrapping behavior not tested | Not automated at any layer | **OPEN** (LOW) -- Add Playwright test: set viewport to 1024px, verify all chips and dropdowns accessible |
| GAP-9.2 | All 10 chips fitting on one row at 1280px not tested | Playwright tests styling classes but not layout overflow | **OPEN** (LOW) -- Add Playwright test: set viewport to 1280px, verify no horizontal overflow in filter bar |

---

### REQ-M7-10: Keyboard Accessibility

**User Story:** As a QA engineer, I want keyboard navigation and operation of all filter bar controls (chips, dropdowns, search) verified, so that users who navigate by keyboard can use all filtering features without a mouse.

#### Acceptance Criteria

1. **AC-M7-10.1:** WHEN navigating with Tab key, THEN focus SHALL move through chips in order, then to the measure dropdown, then to the insurance group dropdown, then to the search input.
2. **AC-M7-10.2:** WHEN a chip has focus and user presses Enter or Space, THEN the chip SHALL toggle its selected state.
3. **AC-M7-10.3:** WHEN the measure dropdown has focus and user presses Enter or Space, THEN it SHALL open.
4. **AC-M7-10.4:** WHEN the insurance group dropdown has focus and user interacts with arrow keys, THEN it SHALL navigate between options.
5. **AC-M7-10.5:** WHEN the search input is focused and user presses Escape, THEN search text SHALL be cleared and input SHALL blur.
6. **AC-M7-10.6:** All chips SHALL have `aria-pressed` attribute reflecting toggle state.
7. **AC-M7-10.7:** The measure dropdown SHALL have `aria-label="Filter by quality measure"`.
8. **AC-M7-10.8:** The insurance group dropdown SHALL have `aria-label="Filter by insurance group"`.
9. **AC-M7-10.9:** The search input SHALL have `aria-label="Search patients by name"`.
10. **AC-M7-10.10:** The clear search button SHALL have `aria-label="Clear search"`.

#### Coverage Gap Analysis (REQ-M7-10)

| Gap ID | Description | Current State | Status |
|--------|------------|---------------|--------|
| GAP-10.1 | Full Tab order through all filter bar controls not tested E2E | Cypress tests chip focus; Vitest tests aria-labels; no full Tab sequence test | **OPEN** (LOW) -- Add Playwright test: Tab through entire filter bar, verify focus order: chips -> measure dropdown -> insurance dropdown -> search input |
| GAP-10.2 | Keyboard operation of dropdowns (Enter, Arrow, Escape) not tested E2E | No E2E keyboard-only dropdown test | **OPEN** (LOW) -- Add Playwright test: Tab to measure dropdown, Enter to open, Arrow Down to select, Enter to confirm |
| GAP-10.3 | Enter/Space toggles chip not explicitly tested E2E | Focus is tested but not keyboard activation | **OPEN** (LOW) -- Add Playwright test: focus chip with Tab, press Enter, verify toggle state changes |

---

## Non-Functional Requirements

### Performance

- **NFR-M7-1:** All Vitest test suites in this module (StatusFilterBar + MainPage filter tests) SHALL complete in under 10 seconds total.
- **NFR-M7-2:** Each individual Cypress E2E test SHALL complete in under 30 seconds (including grid load).
- **NFR-M7-3:** The full Cypress filtering suite SHALL complete in under 10 minutes across all filter-related files.
- **NFR-M7-4:** Filter state changes (chip click, measure selection, search typing) SHALL update the grid within 100ms for datasets up to 5,000 rows (client-side operations; verified by absence of visual delay in E2E tests).

### Reliability

- **NFR-M7-5:** Tests SHALL be deterministic -- no flaky tests due to timing, animation, or data ordering. All Cypress tests SHALL use `cy.waitForAgGrid()` or data-dependent waits, not arbitrary `cy.wait()` delays.
- **NFR-M7-6:** Tests SHALL use unique identifiers or data-driven assertions (e.g., read actual row count from chip, verify grid matches) rather than hardcoded expected counts that may drift with seed data changes.
- **NFR-M7-7:** Insurance group filter tests SHALL handle the asynchronous data fetch (server-side) and wait for the grid to update before making assertions.

### Maintainability

- **NFR-M7-8:** All new test cases SHALL follow existing patterns in their respective test files (e.g., `defaultCounts` fixture in StatusFilterBar.test.tsx, `filterRows()` helper in MainPage.test.tsx, `cy.waitForAgGrid()` in Cypress files).
- **NFR-M7-9:** Combined filter scenario tests SHALL be organized in a dedicated `describe('Combined Filter Scenarios')` block within existing Cypress files rather than creating a new file, unless additions exceed 50 tests.
- **NFR-M7-10:** Test data tables SHALL use parameterized patterns (`forEach`, `it.each`) where more than 3 similar assertions exist.

### Usability

- **NFR-M7-11:** All Cypress E2E tests SHALL be runnable with `--headed` for visual debugging per project conventions.
- **NFR-M7-12:** Test file organization SHALL follow existing naming conventions: `*.test.tsx` for Vitest, `*.cy.ts` for Cypress, `*.spec.ts` for Playwright.

### Security

- **NFR-M7-13:** No security impact. All tests exercise existing filtering behavior on already-fetched data or standard query parameter passing. No new API endpoints or data exposure.

---

## Integration Requirements

- **INT-M7-1:** New Vitest tests for StatusFilterBar behavior SHALL extend `StatusFilterBar.test.tsx` rather than creating a new file.
- **INT-M7-2:** New Vitest tests for filter logic (combined scenarios, measure-scoped counts) SHALL extend `MainPage.test.tsx`.
- **INT-M7-3:** New Cypress tests for combined filter scenarios SHALL be added to a new describe block in `sorting-filtering.cy.ts` or `multi-select-filter.cy.ts` if the additions are fewer than 30 tests. If more, create a new file `combined-filter-scenarios.cy.ts`.
- **INT-M7-4:** New Cypress tests for insurance group gaps SHALL extend `insurance-group-filter.cy.ts`.
- **INT-M7-5:** New Playwright tests for keyboard accessibility and navigation-based filter reset SHALL extend `compact-filter-bar.spec.ts` or be placed in a new `filter-accessibility.spec.ts`.
- **INT-M7-6:** All Cypress tests SHALL use existing custom commands: `cy.login`, `cy.waitForAgGrid`, `cy.addTestRow`, `cy.selectAgGridDropdown`, `cy.getAgGridCell`.
- **INT-M7-7:** All tests SHALL login as admin (`admin@gmail.com` / `welcome100`) unless specifically testing role-based filter behavior (e.g., STAFF physician selector + insurance group interaction).

---

## Assumptions and Constraints

### Assumptions

- **ASM-M7-1:** No production code changes are required for this module. All work is test-only (new tests + updates to existing test files).
- **ASM-M7-2:** The seeded test database contains patients across multiple quality measures, status colors, and insurance groups sufficient to exercise all filter combinations.
- **ASM-M7-3:** All identified coverage gaps are based on file analysis as of the current commit (6e5730a). Gaps may change if tests are added between now and implementation.
- **ASM-M7-4:** Cypress tests require the application to be running locally (`docker-compose up` or `npx vite dev --host` for frontend + backend).
- **ASM-M7-5:** The insurance group filter triggers a server-side re-fetch (`GET /api/data?insuranceGroup=...`), while all other filters (color, measure, search) are client-side on already-loaded data.
- **ASM-M7-6:** The 14 quality measures are: Annual Wellness Visit, Diabetic Eye Exam, Diabetic Nephropathy, Hypertension Management, Diabetes Control, ACE/ARB in DM or CAD, Vaccination, GC/Chlamydia Screening, Breast Cancer Screening, Colon Cancer Screening, Cervical Cancer Screening, Chronic Diagnosis Code, Annual Serum K&Cr, Depression Screening.
- **ASM-M7-7:** Filter state is held in `MainPage` via `useState` hooks: `activeFilters`, `searchText`, `selectedMeasure`, `selectedInsuranceGroup`. These reset on component unmount (navigation away).
- **ASM-M7-8:** The search filtering uses multi-word matching where each space-separated word must be present in the `memberName` (AND logic between words, substring match).

### Constraints

- **CON-M7-1:** AG Grid Community edition does not support native column-level filters. All filtering is implemented as external filtering via `StatusFilterBar` props and `filteredRowData` useMemo in `MainPage`.
- **CON-M7-2:** Insurance group is the only server-side filter; all others (color, measure, search) are client-side.
- **CON-M7-3:** Chip counts are scoped by the selected quality measure but NOT by the active color chips or search text. This is by design so users can see the full distribution within a measure scope.
- **CON-M7-4:** Filter state is not persisted across page refreshes or sessions. This is existing behavior and is not changing.
- **CON-M7-5:** Cypress has known issues with native `<select>` dropdowns for value verification. Tests should use `cy.get('select').should('have.value', 'x')` rather than trying to open the dropdown visually.
- **CON-M7-6:** The `getRowStatusColor()` function is the single source of truth for row color determination, shared between `StatusFilterBar` (for chip counts) and `PatientGrid` (for row CSS classes). Tests should reference this function where applicable.

---

## Edge Cases

- **EDGE-M7-1:** All chips manually selected with a measure filter active: Do NOT auto-switch to "All". All chips show checked. Grid shows all rows for the selected measure (equivalent to "All" but with all chips visually checked).
- **EDGE-M7-2:** Search text containing special regex characters (e.g., parentheses, dots, brackets): The `String.includes()` method used in production is not regex-based, so special characters are treated literally. Tests should verify this.
- **EDGE-M7-3:** Empty memberName (null): Rows with null memberName should be excluded from search results when a search term is active, but included when search is empty.
- **EDGE-M7-4:** Rows with null qualityMeasure: When a specific measure is selected, these rows are excluded. When "All Measures" is active, they are included in counts and filtering.
- **EDGE-M7-5:** Data reload with active filters: When data is refreshed (cell edit, add row, delete row, socket-based refresh), the active filter selections are preserved and re-applied to the updated dataset.
- **EDGE-M7-6:** Rapid measure switching: Quickly changing the measure dropdown multiple times in succession should result in the grid and chip counts reflecting the final selection with no stale state.
- **EDGE-M7-7:** Insurance group filter active during import: If an import completes while a specific insurance group is selected, newly imported patients from a different group will not appear until the user changes the filter.
- **EDGE-M7-8:** Chip with count changing from zero to non-zero after data edit: If a row is edited to a status that previously had zero rows, the chip count should increment and the dashed border should change to normal border styling.
- **EDGE-M7-9:** Pinned row (from add/duplicate) visible despite filter mismatch: A pinned row passes through all filter dimensions until the pin is cleared by user interaction (clicking unpin badge) or changing any filter.
- **EDGE-M7-10:** Filter summary text with multi-color selection: When multiple color chips are selected, the filter summary should list all active colors comma-separated (e.g., "Color: In Progress, Overdue").

---

## Comprehensive Coverage Matrix

> **Last updated:** 2026-02-27 (post `filter-roles-combined.cy.ts` addition, commit 6e5730a)

### Coverage by Acceptance Criterion

Legend: **FULL** = all aspects tested | **PARTIAL** = some aspects tested | **NONE** = no test coverage | **CLOSED** = gap filled by recent test additions

#### REQ-M7-1: Status Color Filter (Multi-Select Toggle)

| AC | Description | Vitest | Cypress | Playwright | Coverage |
|----|------------|--------|---------|------------|----------|
| AC-M7-1.1 | 10 chips appear in correct order | SFB: `renders all filter chips` | SF: 10 individual chip display tests | CFB: `compact chips render` | **FULL** |
| AC-M7-1.2 | Chips display label + count "(X)" | SFB: `displays correct counts on chips` | SF: `should show count on All filter`, `should show counts on each status filter` | -- | **FULL** |
| AC-M7-1.3 | Page load: All active, others inactive | SFB: `treats empty activeFilters as all selected` | MSF: `should start fresh selection when clicking chip while All is active` (verifies All initially pressed) | -- | **FULL** |
| AC-M7-1.4 | Click unselected chip adds it (multi-select ON) | SFB: `clicking unselected chip adds it to active filters` | MSF: `should select multiple chips and show combined rows (OR logic)` | -- | **FULL** |
| AC-M7-1.5 | Click selected chip removes it (toggle OFF) | SFB: `clicking selected chip removes it without affecting others` | MSF: `should toggle off a chip without affecting others` | -- | **FULL** |
| AC-M7-1.6 | Last chip toggled OFF falls back to All | SFB: `toggling off the last selected chip returns to all` | MSF: `should fall back to All when last chip is toggled off` | -- | **FULL** |
| AC-M7-1.7 | All chip clears all selections | SFB: `clicking All chip calls onFilterChange with all` | MSF: `should clear all selections when All is clicked` | -- | **FULL** |
| AC-M7-1.8 | Click chip while All active deselects All | SFB: `clicking chip while All is active selects only that chip` | MSF: `should start fresh selection when clicking chip while All is active` | -- | **FULL** |
| AC-M7-1.9 | Duplicates exclusive mode | SFB: `clicking Duplicates deselects all color chips` | MSF: `should deselect color chips when Duplicates is clicked` | -- | **FULL** |
| AC-M7-1.10 | Color chip exits Duplicates mode | SFB: `clicking color chip while Duplicates active exits duplicates mode` | MSF: `should exit Duplicates mode when color chip is clicked` | -- | **FULL** |
| AC-M7-1.11 | Duplicates toggled OFF falls back to All | SFB: `toggling off Duplicates returns to all` | MSF: `should toggle Duplicates off to All` | -- | **FULL** |
| AC-M7-1.12 | Multi-select OR logic in grid | MP: `filters by multiple colors with OR logic`, `filters by three colors with OR logic` | MSF: `should select multiple chips and show combined rows (OR logic)` | -- | **FULL** |
| AC-M7-1.13 | Selected chip: filled bg + checkmark | SFB: `active chip has filled background`, `active chip still shows checkmark icon` | MSF: `should show checkmark icon on active chips`, `should show checkmarks on multiple active chips` | -- | **FULL** |
| AC-M7-1.14 | Unselected chip: white bg, bordered, no checkmark | SFB: `inactive chip has hover:bg-gray-50 class` | MSF: `should show checkmark icon on active chips` (verifies inactive has no SVG) | -- | **FULL** |
| AC-M7-1.15 | Enter/Space toggles chip (keyboard) | SFB: `clears search and blurs input on Escape key` (Escape tested) | MSF: `should allow focusing chip buttons` | -- | **PARTIAL** (Enter/Space toggle not explicitly tested) |
| AC-M7-1.16 | Tab key navigates chips in order | SFB: `filter chip buttons have focus-visible ring classes` (focus ring exists) | MSF: `should have all chips as focusable buttons with aria-pressed` | -- | **PARTIAL** (focusability tested, tab ORDER not tested) |
| AC-M7-1.17 | aria-pressed updates on toggle | SFB: `active chip has aria-pressed true`, `inactive chip has aria-pressed false` | MSF: `should have correct aria-pressed on All chip`, SF: `should highlight active filter chip with aria-pressed and checkmark` | -- | **FULL** |

#### REQ-M7-2: Chip Count Badges and Accuracy

| AC | Description | Vitest | Cypress | Playwright | Coverage |
|----|------------|--------|---------|------------|----------|
| AC-M7-2.1 | All Measures: counts reflect full dataset | MP: `"All Measures" counts all rows` | SF: `should show counts on each status filter` | -- | **FULL** |
| AC-M7-2.2 | Specific measure: counts scoped | MP: `scopes chip counts to selected measure` | CFB: `chip counts update when quality measure is selected` | -- | **FULL** |
| AC-M7-2.3 | Measure-scoped All chip = total for that measure | MP: `scopes chip counts to selected measure` (implicit) | -- | -- | **PARTIAL** (only Vitest, no E2E) |
| AC-M7-2.4 | Search text does not affect chip counts | MP: `rowCounts are computed from unfiltered rowData` | MSF: `should preserve chip counts regardless of active filters` | -- | **FULL** |
| AC-M7-2.5 | Status edit updates chip counts immediately | -- | FRC: `editing measureStatus changes chip counts` (partial -- verifies counts exist, does not verify specific delta) | -- | **PARTIAL** |
| AC-M7-2.6 | Overdue row counted under red chip | SFB-R8: `overdue row counted as red, NOT its natural color` | -- | -- | **PARTIAL** (Vitest only) |
| AC-M7-2.7 | Duplicate+overdue counted under both chips | SFB-R8: `duplicate + overdue counted in BOTH duplicate AND red` | -- | -- | **PARTIAL** (Vitest only) |
| AC-M7-2.8 | All count = sum of color counts (excludes duplicate) | SFB-R8: `"All" total excludes duplicate count to avoid double-counting` | FRC: `sum of all color chip counts equals All chip count` | -- | **FULL** |
| AC-M7-2.9 | Zero-count chip has dashed border | SFB: `zero-count inactive chip has border-dashed` | -- | CFB-PW: `zero-count chip appears faded` | **FULL** |
| AC-M7-2.10 | Zero-count chip clickable, shows empty grid | SFB: `zero-count chip is still clickable` | -- | CFB-PW: `zero-count chip appears faded` (verifies not disabled) | **PARTIAL** (click behavior tested at Vitest, empty grid not verified E2E) |

#### REQ-M7-3: Insurance Group Filter

| AC | Description | Vitest | Cypress | Playwright | Coverage |
|----|------------|--------|---------|------------|----------|
| AC-M7-3.1 | Dropdown renders with aria-label | SFB: `renders insurance group dropdown` | IGF: `should render insurance group dropdown with aria-label` | -- | **FULL** |
| AC-M7-3.2 | Options order: All, systems (alpha), No Insurance | SFB: `shows "All" option first`, `shows system options sorted alphabetically`, `shows "No Insurance" option last` | IGF: `should show "All" option first`, `should show "No Insurance" option last`, `should show system options between All and No Insurance` | -- | **FULL** |
| AC-M7-3.3 | Default to "Hill" | SFB: `default selection is "hill"` | IGF: `should default to "Hill" selection` | -- | **FULL** |
| AC-M7-3.4 | Selecting group passes insuranceGroup query param | MP: `API request includes insuranceGroup=hill by default` | -- | -- | **PARTIAL** (Vitest unit test only) |
| AC-M7-3.5 | "No Insurance" passes insuranceGroup=none | MP: `API includes insuranceGroup=none when "none" selected` | FRC: `"No Insurance" filter shows correct rows or empty grid` | -- | **FULL** |
| AC-M7-3.6 | "All" omits insuranceGroup param | MP: `API omits insuranceGroup when set to "all"` | IGF: `should change data when selecting "All"` | -- | **FULL** |
| AC-M7-3.7 | Active filter: blue ring indicator | SFB: `shows active-ring indicator when value is not "all"` | IGF: `should show active-ring when filter is not "All"` | -- | **FULL** |
| AC-M7-3.8 | "All": no blue ring | SFB: `does not show active-ring when value is "all"` | IGF: `should remove active-ring when "All" is selected` | -- | **FULL** |
| AC-M7-3.9 | Insurance change updates chip counts | -- | FRC: `insurance group + color chip: switching insurance group updates chip counts` | -- | **FULL** (CLOSED -- was GAP-3.4) |
| AC-M7-3.10 | API failure: fallback options | MP: `falls back to hardcoded options on fetch failure` | -- | -- | **PARTIAL** (Vitest only, no E2E) |
| AC-M7-3.11 | Insurance change preserves other filters | MP: `changing insuranceGroup changes query params` (implicit) | IGF: `should persist insurance group filter when quality measure changes` | -- | **FULL** |

#### REQ-M7-4: Patient Name Search

| AC | Description | Vitest | Cypress | Playwright | Coverage |
|----|------------|--------|---------|------------|----------|
| AC-M7-4.1 | Search input with placeholder and icon | SFB: `renders search input with placeholder`, `has aria-label on search input` | PNS: `should display search input with placeholder` | -- | **FULL** |
| AC-M7-4.2 | Empty search: no filtering, no clear button | SFB: `does NOT show clear button when searchText is empty` | PNS: `should not show clear button when search is empty` | -- | **FULL** |
| AC-M7-4.3 | Typing filters by memberName (case-insensitive, substring) | MP: `filters by name (case-insensitive)`, `filters by name (partial match)`, `matches any part of the name` | PNS: `should filter grid rows when typing a patient name`, `should be case-insensitive`, `should support partial match` | -- | **FULL** |
| AC-M7-4.4 | Multi-word search: all words must match | MP: `matches multi-word search across "lastname, firstname" format`, `matches multi-word search in any order`, `requires all words to match` | -- | -- | **PARTIAL** (Vitest only, no E2E) |
| AC-M7-4.5 | Zero matches: empty grid | MP: `returns empty when no names match` | PNS: `should show empty grid when no names match` | -- | **FULL** |
| AC-M7-4.6 | Clear restores rows (respecting other filters) | MP: `clearing search with active status filter restores status-filtered set` | PNS: `should restore all rows when clear button is clicked` | -- | **FULL** |
| AC-M7-4.7 | Clear (X) button appears when text entered | SFB: `shows clear button when searchText is non-empty` | PNS: `should show clear button when search has text` | -- | **FULL** |
| AC-M7-4.8 | Clear button clears text and restores rows | SFB: `calls onSearchChange with empty string when clear button clicked` | PNS: `should restore all rows when clear button is clicked`, `should hide clear button after clearing` | -- | **FULL** |
| AC-M7-4.9 | Ctrl+F focuses search input | -- | PNS: `should focus search input on Ctrl+F` | -- | **FULL** |
| AC-M7-4.10 | Escape clears search and blurs | SFB: `clears search and blurs input on Escape key` | PNS: `should clear search and blur on Escape` | -- | **FULL** |
| AC-M7-4.11 | Search + color filter = AND logic | MP: `applies both filters: green + "smith"` | PNS: `should apply both status filter and name search`, MSF: `should apply search AND multi-filter together` | -- | **FULL** |
| AC-M7-4.12 | Aria labels on search + clear button | SFB: `has aria-label on search input`, `has aria-label on clear button` | PNS: `should display search input with placeholder` (checks aria-label) | -- | **FULL** |

#### REQ-M7-5: Quality Measure Filter

| AC | Description | Vitest | Cypress | Playwright | Coverage |
|----|------------|--------|---------|------------|----------|
| AC-M7-5.1 | Dropdown with aria-label after chips + divider | SFB: `renders measure dropdown`, `has aria-label on dropdown`, `renders vertical divider before dropdown` | -- | CFB-PW: `measure dropdown is visible and functional` | **FULL** |
| AC-M7-5.2 | Default: "All Measures" | SFB: `dropdown default is "All Measures"` | -- | CFB-PW: `measure dropdown is visible and functional` | **FULL** |
| AC-M7-5.3 | 14 quality measures + All Measures = 15 options | SFB: `dropdown lists all 13 quality measures` (actually verifies 15 options including Depression Screening) | -- | -- | **PARTIAL** (Vitest only, no E2E verification of option count) |
| AC-M7-5.4 | Selecting measure filters grid | MP: `filters rows by selected measure` | CFB: `grid shows correct rows when measure is selected` | -- | **FULL** |
| AC-M7-5.5 | Active measure: blue border highlight | SFB: `shows blue ring when measure is active` | -- | CFB-PW: `measure dropdown is visible and functional` (checks ring classes) | **FULL** |
| AC-M7-5.6 | All Measures: no highlight | SFB: `has no blue ring when "All Measures" selected` | -- | -- | **FULL** |
| AC-M7-5.7 | Measure-scoped chip counts | MP: `scopes chip counts to selected measure`, `Diabetic Eye Exam counts only DEE rows` | CFB: `chip counts update when quality measure is selected` | -- | **FULL** |
| AC-M7-5.8 | All Measures clears measure filter | MP: `"All Measures" shows all rows` | CFB: `grid shows correct rows when measure is selected` (resets to All) | -- | **FULL** |
| AC-M7-5.9 | Measure + color chips = AND + OR | MP: `measure + color filter applies AND logic` | CFB: `measure dropdown + color chip applies AND filter to grid` | -- | **FULL** |

#### REQ-M7-6: Combined Filter Scenarios

| AC | Description | Vitest | Cypress | Playwright | Coverage |
|----|------------|--------|---------|------------|----------|
| AC-M7-6.1 | All 4 filters AND logic | MP: `measure + color + search applies triple AND` | FRC: `applies all 4 filters simultaneously (color + QM + insurance + search)` | -- | **FULL** (CLOSED -- was GAP-6.2) |
| AC-M7-6.2 | Changing one filter preserves others | MP: `changing measure preserves active color filter` | FRC: `removing one filter restores rows while others stay active` | -- | **FULL** (CLOSED -- was GAP-6.5) |
| AC-M7-6.3 | Color OR + other AND logic | MP: `multi-filter + search applies AND logic` | MSF: `should select multiple chips and show combined rows (OR logic)` | -- | **FULL** |
| AC-M7-6.4 | Duplicates + measure + search AND | MP: `duplicate filter + measure applies AND logic` | -- | -- | **PARTIAL** (Vitest only, no E2E) |
| AC-M7-6.5 | Zero-result combined filter: empty grid + status bar | MP: `multi-filter + search with no matching combo returns empty` | FRC: `applies all 4 filters simultaneously` (verifies 0 rows with nonexistent search) | -- | **FULL** |
| AC-M7-6.6 | Color + measure filter summary in status bar | -- | -- | CFB-PW: `combined filter updates status bar summary` (verifies Completed + AWV in summary) | **FULL** |
| AC-M7-6.7 | Insurance + color + measure filter summary | MP: filterSummary tests (insurance+color, insurance+measure) | -- | -- | **PARTIAL** (Vitest only, no E2E for triple summary) |
| AC-M7-6.8 | All defaults = no filter summary | MP: `filterSummary omits insurance group when "all"` | -- | CFB-PW: `combined filter updates status bar summary` (verifies no summary after reset) | **FULL** |

#### REQ-M7-7: Filter State Management

| AC | Description | Vitest | Cypress | Playwright | Coverage |
|----|------------|--------|---------|------------|----------|
| AC-M7-7.1 | Initial state: All, All Measures, empty search, Hill | SFB: multiple tests | IGF: `should default to "Hill" selection`, MSF: `should start fresh selection` | CFB-PW: `measure dropdown is visible and functional` | **FULL** |
| AC-M7-7.2 | Navigation away/back resets filters | -- | FRC: `filter state persists after page navigation and back` (verifies grid loads; notes filter reset) | -- | **PARTIAL** (tested but assertion is soft -- verifies no crash, not specific reset) |
| AC-M7-7.3 | Physician selector change preserves insurance | -- | FRC-STAFF: `STAFF can switch physician and chip counts update` (tests physician switch) | -- | **PARTIAL** (tests physician switch effect on counts, not explicitly insurance preservation) |
| AC-M7-7.4 | Insurance change triggers server re-fetch | MP: `changing insuranceGroup changes query params (triggering re-fetch)` | -- | -- | **PARTIAL** (Vitest only) |
| AC-M7-7.5 | Data refresh preserves filter selections | -- | -- | -- | **NONE** |
| AC-M7-7.6 | Filter change clears pinned row badge | SFB: pinned row badge tests, MP: `clearing pin re-applies all filters` | -- | -- | **PARTIAL** (Vitest only) |
| AC-M7-7.7 | Pinned row bypasses all filters | MP: `pinned row passes through all combined filters` | -- | -- | **PARTIAL** (Vitest only, no E2E) |

#### REQ-M7-8: Status Bar Filter Display

| AC | Description | Vitest | Cypress | Playwright | Coverage |
|----|------------|--------|---------|------------|----------|
| AC-M7-8.1 | "Showing X of Y rows" with filters active | SB: `shows "Showing X of Y rows" when filtered` | SF: `should update status bar count when filtering`, PNS: `should update row count when search is active` | -- | **FULL** |
| AC-M7-8.2 | Color filter summary text | SB: `shows filter summary when provided` | -- | CFB-PW: `combined filter updates status bar summary` | **FULL** |
| AC-M7-8.3 | Measure filter summary text | SB: `shows combined summary with pipe separator` | -- | CFB-PW: `combined filter updates status bar summary` | **FULL** |
| AC-M7-8.4 | Insurance filter summary text | MP: `filterSummary includes "Insurance: Hill" when active` | -- | -- | **PARTIAL** (Vitest only) |
| AC-M7-8.5 | All defaults = no filter summary | MP: `filterSummary omits insurance group when "all"` | -- | CFB-PW: `combined filter updates status bar summary` (resets and verifies) | **FULL** |
| AC-M7-8.6 | Triple filter summary with pipe separators | MP: `filterSummary combines insurance with color filter`, `filterSummary combines insurance with measure filter` | -- | -- | **PARTIAL** (Vitest has insurance+color and insurance+measure pairs, no triple E2E) |
| AC-M7-8.7 | Search narrows count but not in summary | SB: `does not show filter summary when undefined` | PNS: `should update row count when search is active` | -- | **FULL** |

#### REQ-M7-9: Compact Chip Styling

| AC | Description | Vitest | Cypress | Playwright | Coverage |
|----|------------|--------|---------|------------|----------|
| AC-M7-9.1 | whitespace-nowrap on chips | SFB: `chips have whitespace-nowrap class` | -- | CFB-PW: `compact chips render on single line` | **FULL** |
| AC-M7-9.2 | Compact padding py-0.5 px-2, text-xs | SFB: `chips have compact padding`, `chips use text-xs for compact font` | -- | CFB-PW: `compact chips render on single line` | **FULL** |
| AC-M7-9.3 | Multi-word labels stay on single line | -- | -- | CFB-PW: `compact chips render on single line` (verifies all 10 visible) | **PARTIAL** (no explicit truncation check) |
| AC-M7-9.4 | Compact chips preserve visual treatment | SFB: `active chip still shows checkmark icon` | -- | -- | **FULL** |
| AC-M7-9.5 | No overflow at 1280px+ | -- | -- | -- | **NONE** |

#### REQ-M7-10: Keyboard Accessibility

| AC | Description | Vitest | Cypress | Playwright | Coverage |
|----|------------|--------|---------|------------|----------|
| AC-M7-10.1 | Tab order: chips -> measure -> insurance -> search | SFB: `filter chip buttons have focus-visible ring classes` | -- | -- | **PARTIAL** (focus ring exists, tab order not verified) |
| AC-M7-10.2 | Enter/Space toggles chip | -- | -- | -- | **NONE** (no explicit E2E keyboard toggle test) |
| AC-M7-10.3 | Enter/Space opens measure dropdown | -- | -- | -- | **NONE** |
| AC-M7-10.4 | Arrow keys navigate dropdown options | -- | -- | -- | **NONE** |
| AC-M7-10.5 | Escape clears search and blurs | SFB: `clears search and blurs input on Escape key` | PNS: `should clear search and blur on Escape` | -- | **FULL** |
| AC-M7-10.6 | aria-pressed on all chips | SFB: `active chip has aria-pressed true`, `inactive chip has aria-pressed false` | MSF: `should have all chips as focusable buttons with aria-pressed` | -- | **FULL** |
| AC-M7-10.7 | QM dropdown aria-label | SFB: `has aria-label on dropdown` | -- | -- | **FULL** |
| AC-M7-10.8 | Insurance dropdown aria-label | SFB: `renders insurance group dropdown` | IGF: `should render insurance group dropdown with aria-label` | -- | **FULL** |
| AC-M7-10.9 | Search input aria-label | SFB: `has aria-label on search input` | PNS: `should display search input with placeholder` | -- | **FULL** |
| AC-M7-10.10 | Clear button aria-label | SFB: `has aria-label on clear button` | PNS: `should hide clear button after clearing` | -- | **FULL** |

#### Edge Cases Coverage

| Edge Case | Vitest | Cypress | Playwright | Coverage |
|-----------|--------|---------|------------|----------|
| EDGE-M7-1: All chips manually selected (no auto-All) | MP: `all chips selected shows all rows (equivalent to All)` | -- | -- | **PARTIAL** (Vitest only) |
| EDGE-M7-2: Special regex chars in search | -- | FRC: `search with special characters does not break the grid`, `search with regex metacharacters does not crash` | -- | **FULL** (CLOSED) |
| EDGE-M7-3: Null memberName excluded from search | MP: `excludes rows with null memberName from search results`, `returns null memberName rows when search is empty` | -- | -- | **PARTIAL** (Vitest only) |
| EDGE-M7-4: Null qualityMeasure excluded when measure selected | MP: `excludes rows with null qualityMeasure when measure is selected` | -- | -- | **PARTIAL** (Vitest only) |
| EDGE-M7-5: Data reload preserves filters | -- | -- | -- | **NONE** |
| EDGE-M7-6: Rapid measure switching | -- | FRC: `rapid filter switching does not break the grid` (covers chip rapid switching; measure rapid switch not explicitly tested) | -- | **PARTIAL** |
| EDGE-M7-7: Insurance filter during import | -- | -- | -- | **NONE** (requires import integration) |
| EDGE-M7-8: Chip count zero-to-nonzero after edit | -- | -- | -- | **NONE** |
| EDGE-M7-9: Pinned row bypasses all filters | MP: `pinned row passes through all combined filters` | -- | -- | **PARTIAL** (Vitest only) |
| EDGE-M7-10: Multi-color filter summary | MP: `filterSummary combines insurance with color filter` | -- | -- | **PARTIAL** (Vitest only) |

### Coverage Summary

| Category | Total ACs | FULL | PARTIAL | NONE | % Covered |
|----------|----------|------|---------|------|-----------|
| REQ-M7-1: Status Color Filter | 17 | 14 | 3 | 0 | 82% |
| REQ-M7-2: Chip Count Accuracy | 10 | 5 | 5 | 0 | 50% |
| REQ-M7-3: Insurance Group Filter | 11 | 9 | 2 | 0 | 82% |
| REQ-M7-4: Patient Name Search | 12 | 11 | 1 | 0 | 92% |
| REQ-M7-5: Quality Measure Filter | 9 | 8 | 1 | 0 | 89% |
| REQ-M7-6: Combined Filters | 8 | 6 | 2 | 0 | 75% |
| REQ-M7-7: Filter State Management | 7 | 1 | 5 | 1 | 14% |
| REQ-M7-8: Status Bar Display | 7 | 5 | 2 | 0 | 71% |
| REQ-M7-9: Compact Chip Styling | 5 | 3 | 1 | 1 | 60% |
| REQ-M7-10: Keyboard Accessibility | 10 | 6 | 1 | 3 | 60% |
| Edge Cases | 10 | 1 | 6 | 3 | 10% |
| **TOTAL** | **106** | **69** | **29** | **8** | **65%** |

---

## Updated Gap Analysis (Post filter-roles-combined.cy.ts)

### Gaps CLOSED by filter-roles-combined.cy.ts (24 tests)

The following gaps from the original analysis have been fully or substantially addressed:

| Original Gap ID | Description | How Closed | Remaining Work |
|-----------------|------------|------------|----------------|
| GAP-6.2 | Quadruple combination (insurance + color + measure + search) | FRC: `applies all 4 filters simultaneously` | None -- fully closed |
| GAP-6.5 | Filter dimension preservation when changing one filter | FRC: `removing one filter restores rows while others stay active` | None -- fully closed |
| GAP-3.4 | Chip counts update after insurance group change | FRC: `insurance group + color chip: switching insurance group updates chip counts` | None -- fully closed |
| GAP-2.8 (new) | Sum of color chip counts = All count | FRC: `sum of all color chip counts equals All chip count` | None -- fully closed |
| GAP-3.2 | "No Insurance" E2E | FRC: `"No Insurance" filter shows correct rows or empty grid` | None -- fully closed |
| GAP-1.2 | Rapid chip clicking | FRC: `rapid filter switching does not break the grid` | None -- fully closed |
| EDGE-M7-2 | Special characters in search | FRC: `search with special characters does not break the grid`, `search with regex metacharacters does not crash` | None -- fully closed |

### Remaining Gaps (Ordered by Priority)

#### HIGH Priority (core workflow coverage gaps)

| Gap ID | AC Ref | Description | Current State | Proposed Test | Framework | Target File |
|--------|--------|------------|---------------|---------------|-----------|-------------|
| GAP-2.2 | AC-M7-2.5 | Chip counts after row status edit not deeply tested E2E | FRC has partial test that verifies counts exist but does not assert specific increment/decrement | E2E: edit a row's measureStatus to change color, verify source chip count decrements and target increments | Cypress | `filter-roles-combined.cy.ts` |
| GAP-7.5 | AC-M7-7.5 | Data refresh preserves filter state not tested E2E | Not automated at any layer | E2E: select measure + color filter, edit a cell (triggers data update), verify filter selections preserved after update | Cypress | `filter-roles-combined.cy.ts` |
| GAP-6.4 | AC-M7-6.4 | Duplicates + measure + search triple AND not tested E2E | Only Vitest tests `duplicate filter + measure applies AND logic` | E2E: select Duplicates chip, select a measure, type search, verify only matching rows appear | Cypress | `filter-roles-combined.cy.ts` |
| GAP-8.6 | AC-M7-8.6 | Triple filter summary (insurance + color + measure) not tested E2E | Vitest tests individual and dual combinations; no E2E for triple | E2E: set insurance non-All, select color chip, select measure, verify status bar shows all three | Cypress | `filter-roles-combined.cy.ts` |

#### MEDIUM Priority (defense-in-depth for unit-tested logic)

| Gap ID | AC Ref | Description | Current State | Proposed Test | Framework | Target File |
|--------|--------|------------|---------------|---------------|-----------|-------------|
| GAP-1.1 | AC-M7-1.9-1.11 | Combined multi-select + Duplicates exclusivity full sequence not tested as single E2E flow | Tested separately | E2E: select 2 color chips, click Duplicates (verify colors deselected), click color chip (verify Duplicates deselected), toggle off last chip (verify All) | Cypress | `multi-select-filter.cy.ts` |
| GAP-4.1 | AC-M7-4.4 | Multi-word search ("williams robert") not tested E2E | Only Vitest tests multi-word matching | E2E: read a name from grid, type "lastname firstname" in search, verify row appears | Cypress | `patient-name-search.cy.ts` |
| GAP-4.3 | AC-M7-4.4 | Search with extra whitespace not tested E2E | Only Vitest tests whitespace trimming | E2E: type " smith  john " with extra spaces, verify filtering works correctly | Cypress | `patient-name-search.cy.ts` |
| GAP-4.4 | AC-M7-6.2 | Search text preserved when color filter changes not tested E2E | Only Vitest tests | E2E: type search term, click color chip, verify search input still shows term + combined filtering | Cypress | `patient-name-search.cy.ts` |
| GAP-5.3 | AC-M7-5.3 | Measure dropdown option count (15 total) not tested E2E | Only Vitest tests option count | E2E: open measure dropdown, count options, verify 15 total (All Measures + 14 measures) | Cypress | `compact-filter-bar.cy.ts` |
| GAP-7.3 | AC-M7-7.7 | Pinned row bypass of all filters not tested E2E | Only Vitest tests pinned row bypass | E2E: add a new row (creates pin), select a measure that does not match, verify new row still visible | Cypress | `sorting-filtering.cy.ts` |
| GAP-7.4 | AC-M7-7.6 | Filter change clears pinned row badge not tested E2E | Only Vitest tests callback | E2E: add a row (pinned badge appears), change color filter, verify pinned badge disappears | Cypress | `sorting-filtering.cy.ts` |
| GAP-7.6 | AC-M7-7.5 | Sort order preserved when filter applied not tested E2E | Not tested as explicit scenario | E2E: sort by column, apply color filter, verify filtered rows maintain active sort order | Cypress | `sorting-filtering.cy.ts` |
| GAP-8.4 | AC-M7-8.4 | Insurance filter summary text not tested E2E | Only Vitest tests | E2E: set insurance to "Hill" (or non-All), verify status bar includes "Insurance: Hill" | Cypress | `insurance-group-filter.cy.ts` |
| GAP-2.4 | AC-M7-2.10 | Zero-count chip click shows empty grid not tested E2E | Only Vitest tests clickability | E2E: select a measure where a color has zero rows, click that chip, verify empty grid | Cypress | `compact-filter-bar.cy.ts` |
| GAP-3.3 | AC-M7-3.10 | Insurance dropdown fallback on API failure not tested E2E | Only Vitest tests | E2E: intercept `/api/import/systems` with error, verify dropdown shows fallback options | Playwright | `compact-filter-bar.spec.ts` |
| GAP-7.2 | AC-M7-7.3 | Physician selector change preserves insurance filter not tested E2E | FRC tests physician switch but not insurance preservation explicitly | E2E (ADMIN): set insurance to "All", change physician selector, verify insurance still "All" | Cypress | `filter-roles-combined.cy.ts` |

#### LOW Priority (accessibility and responsive design)

| Gap ID | AC Ref | Description | Current State | Proposed Test | Framework | Target File |
|--------|--------|------------|---------------|---------------|-----------|-------------|
| GAP-9.1 | AC-M7-9.5 | Narrow viewport (<1280px) wrapping behavior not tested | Not automated | E2E: set viewport to 1024px, verify all chips and dropdowns accessible | Playwright | `filter-accessibility.spec.ts` (new) |
| GAP-9.2 | AC-M7-9.5 | All elements fit without overflow at 1280px not tested | Not automated | E2E: set viewport to 1280px, verify no horizontal overflow in filter bar | Playwright | `filter-accessibility.spec.ts` (new) |
| GAP-10.1 | AC-M7-10.1 | Full Tab order through all filter bar controls not tested E2E | No full Tab sequence test | E2E: Tab through entire filter bar, verify focus order | Playwright | `filter-accessibility.spec.ts` (new) |
| GAP-10.2 | AC-M7-10.2-4 | Keyboard-only dropdown operation not tested E2E | No E2E keyboard-only dropdown test | E2E: Tab to measure dropdown, Enter to open, Arrow to select, Enter to confirm | Playwright | `filter-accessibility.spec.ts` (new) |
| GAP-10.3 | AC-M7-1.15 | Enter/Space toggles chip not explicitly tested E2E | Focus is tested but not keyboard activation | E2E: focus chip with Tab, press Enter, verify toggle | Playwright | `filter-accessibility.spec.ts` (new) |

---

## Proposed New Test Case Summary

### By Gap Priority (Updated)

| Priority | Gap IDs | Test Layer | Est. New Tests | Description |
|----------|---------|------------|---------------:|-------------|
| HIGH | GAP-2.2 | Cypress | 1 | Chip count delta after row status edit |
| HIGH | GAP-7.5 | Cypress | 1 | Data refresh preserves filter state |
| HIGH | GAP-6.4 | Cypress | 1 | Duplicates + measure + search triple AND |
| HIGH | GAP-8.6 | Cypress | 1 | Triple filter summary E2E |
| MEDIUM | GAP-1.1 | Cypress | 1 | Combined toggle + Duplicates exclusivity sequence |
| MEDIUM | GAP-4.1, GAP-4.3, GAP-4.4 | Cypress | 3 | Multi-word search, whitespace handling, search preserved on filter change |
| MEDIUM | GAP-5.3 | Cypress | 1 | Measure dropdown option count validation |
| MEDIUM | GAP-7.3, GAP-7.4 | Cypress | 2 | Pinned row bypass E2E, pin clear on filter change |
| MEDIUM | GAP-7.6 | Cypress | 1 | Sort order preserved when filter applied |
| MEDIUM | GAP-8.4 | Cypress | 1 | Insurance filter summary text E2E |
| MEDIUM | GAP-2.4 | Cypress | 1 | Zero-count chip click shows empty grid |
| MEDIUM | GAP-3.3 | Playwright | 1 | API failure fallback options E2E |
| MEDIUM | GAP-7.2 | Cypress | 1 | Physician selector preserves insurance filter |
| LOW | GAP-9.1, GAP-9.2 | Playwright | 2 | Viewport wrapping and overflow tests |
| LOW | GAP-10.1, GAP-10.2, GAP-10.3 | Playwright | 3 | Full Tab order, keyboard dropdown operation, Enter/Space chip toggle |

### Totals

| Layer | Existing Tests | Est. New Tests |
|-------|---------------:|---------------:|
| Vitest (StatusFilterBar.test.tsx) | 181 | 0 (strong unit coverage; all PARTIAL gaps are E2E-level) |
| Vitest (StatusBar.test.tsx) | 18 | 0 |
| Vitest (MainPage.test.tsx) | 67 | 0 (comprehensive filter logic coverage) |
| Cypress (multi-select-filter.cy.ts) | 18 | +1 (GAP-1.1) |
| Cypress (patient-name-search.cy.ts) | 14 | +3 (GAP-4.1, 4.3, 4.4) |
| Cypress (insurance-group-filter.cy.ts) | 12 | +1 (GAP-8.4) |
| Cypress (compact-filter-bar.cy.ts) | 3 | +2 (GAP-5.3, 2.4) |
| Cypress (sorting-filtering.cy.ts) | 56 | +3 (GAP-7.3, 7.4, 7.6) |
| Cypress (filter-roles-combined.cy.ts) | 24 | +5 (GAP-2.2, 7.5, 6.4, 8.6, 7.2) |
| Playwright (compact-filter-bar.spec.ts) | 5 | +1 (GAP-3.3) |
| Playwright (filter-accessibility.spec.ts — NEW) | 0 | +5 (GAP-9.1, 9.2, 10.1, 10.2, 10.3) |
| **TOTAL** | **~398** | **+21** |

**Post-implementation expected totals:** ~398 existing + ~21 new = **~419 tests** in Module 7 scope.

### Priority Breakdown

| Priority | Tests | Rationale |
|----------|------:|-----------|
| HIGH | 4 | Chip count accuracy after edits, data refresh filter preservation, Duplicates+measure+search triple AND, and triple filter summary are the remaining critical workflow gaps. These test real-world scenarios where users edit data while filters are active. |
| MEDIUM | 12 | Individual filter dimension gaps (multi-word search E2E, pinned row E2E, dropdown option count, filter persistence across physician switch) provide defense-in-depth for already-unit-tested logic. Most of these are "lift Vitest coverage to E2E" tests. |
| LOW | 5 | Viewport layout (responsive design), full keyboard Tab order, and keyboard-only dropdown operation. Lower risk because the underlying controls are standard HTML elements with native keyboard support, but important for accessibility compliance. |
