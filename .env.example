# ===========================================
# Patient Tracker - Environment Configuration
# ===========================================
# Copy this file to .env and fill in the values

# ------------------------------------------
# REQUIRED - You must set these
# ------------------------------------------

# Database password (make it secure!)
DB_PASSWORD=your_secure_password_here

# JWT secret for authentication (generate with: openssl rand -hex 32)
JWT_SECRET=your_64_character_random_string_here

# ------------------------------------------
# OPTIONAL - Defaults are usually fine
# ------------------------------------------

# Environment mode (production recommended for deployment)
NODE_ENV=production

# JWT token expiration time
JWT_EXPIRES_IN=8h

# Public URL of your application (used in password reset emails)
APP_URL=http://localhost

# ------------------------------------------
# OPTIONAL - SMTP for Password Reset Emails
# ------------------------------------------
# If not configured, "Forgot Password" will show "Contact Administrator"

# SMTP_HOST=mail.yourcompany.com
# SMTP_PORT=587
# SMTP_SECURE=false
# SMTP_USER=noreply@yourcompany.com
# SMTP_PASS=your_email_password
# SMTP_FROM=Patient Tracker <noreply@yourcompany.com>
